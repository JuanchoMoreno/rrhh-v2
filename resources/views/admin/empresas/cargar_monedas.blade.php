{{-- <select name="moneda" id="select_moneda" class="form-control">
    @foreach ($monedas as $moneda)
        <option value="{{ $moneda->symbol }}">{{ $moneda->code }}
        </option>
    @endforeach
</select> --}}